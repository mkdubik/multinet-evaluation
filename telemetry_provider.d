provider multinet_evaluation {

  /*probe lart(char* dataset, int t, char *eps, char* gamma);
  probe glouvain(char* dataset, char* method, char *gamma, char* omega);
  probe pmm(char* dataset, int k, int ell);*/

  probe start();
  probe finished();

};
